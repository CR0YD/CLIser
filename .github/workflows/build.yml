name: Build pipeline

on:
  pull_request:
    branches: [ 'master' ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Execute cmake
        run: cmake .
      - name: Build cliser library
        run: make cliser
